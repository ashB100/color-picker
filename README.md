# ColorPicker

Uses the native `<input type="color">` to pick a color and displays its hex value. 
## Utilising Angular's:
* Property binding
* @Input() decorator
* Event binding
* @Output() decorator
* EventEmitter



